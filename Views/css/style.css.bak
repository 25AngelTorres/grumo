/*Variables personalizado*/
:root {
    --mainBackg1: 	#eee;
	--mainBackg2:	#cbd9da;
	--mainBackg3:	#fff;
	--mainResalte:	#27494c!important;
	--mainResalte2:	#27494c!important;
}


/*Estilos para login*/
.log_form{
	padding-left: 30%;
	padding-right: 30%;
}
.btn-log_form{
	color: var(--mainBackg1);
	background: var(--mainResalte);
	border-color: var(--mainResalte);
	
}
.resalte1{
	background-color: var(--mainResalte);
}

/*Estilos para menu bar y barra lateral
.resalte1{
	background-color: var(--mainResalte);
}
.sidebar{
	position: absolute;
	width:200px;
	high: 100%;
	background: var(--mainBackg1);
	color: var(--mainResalte);
	
}
.sidebar ul{
	list-style: none;
	padding: 0;
	margin: 0;
}
.sidebar li{
	border: 1px solid #F1F2F3;
	transition: all 0.3s;
}
.sidebar li:hover{
	background-color: var(--mainBackg2);
	border-left: 5px solid #27494c!important;
}
.sidebar a{
	display: block;
	padding: 10px;
	color: black;
	text-decoration: none;
}


.contenido{
    width: -webkit-calc(100% - 50px);
    width:    -moz-calc(100% - 50px);
    width:         calc(100% - 50px);
	height: 100%;
	position: absolute;
	background-color: var(--mainBackg3);
	margin-left: 30px;
	transition: all 0.3s;
}

.menu-sidebar{
	cursor: pointer;
}

.abrir{
	margin-left:200px;
	width: -webkit-calc(100% - 200px);
    width:    -moz-calc(100% - 200px);
    width:         calc(100% - 200px);
}
*/





.sidebar {
  width: 90px !important;
  background-color: var(--mainResalte2);
  min-height: calc(100vh - 56px);
}

.sidebar .nav-item:last-child {
  margin-bottom: 1rem;
}

.sidebar .nav-item .nav-link {
  text-align: center;
  padding: 0.75rem 1rem;
  width: 90px;
}

.sidebar .nav-item .nav-link span {
  font-size: 0.65rem;
  display: block;
}

.sidebar .nav-item .dropdown-menu {
  position: absolute !important;
  -webkit-transform: none !important;
  transform: none !important;
  left: calc(90px + 0.5rem) !important;
  margin: 0;
  word-wrap:break-word;
}
.dropdown-item{
	white-space: normal;
}

.sidebar .nav-item .dropdown-menu.dropup {
  bottom: 0;
  top: auto !important;
}

.sidebar .nav-item.dropdown .dropdown-toggle::after {
  display: none;
}


.sidebar .nav-item .nav-link {
  color: rgba(255, 255, 255, 0.5);
}

.sidebar .nav-item .nav-link:active, .sidebar .nav-item .nav-link:focus, .sidebar .nav-item .nav-link:hover {
  color: rgba(255, 255, 255, 0.75);
}

.sidebar.toggled {
  width: 0 !important;
  overflow: hidden;
}

.dropdown-item a{
	width:			100%;
	padding:		.25rem 1.5rem;
	clear:			both;
	font-weight:	300;
	color:			#000;
	text-align:		inherit;
	white-space:	normal;
	background-color:transparent;
	display: block;
}

@media (min-width: 768px) {
  .sidebar {
    width: 225px !important;
  }
  .sidebar .nav-item .nav-link {
    display: block;
    width: 100%;
    text-align: left;
    padding: 1rem;
    width: 225px;
  }
  .sidebar .nav-item .nav-link span {
    font-size: 1rem;
    display: inline;
  }
  .sidebar .nav-item .dropdown-menu {
    position: static !important;
    margin: 0 1rem;
    top: 0;
  }
  .sidebar .nav-item.dropdown .dropdown-toggle::after {
    display: block;
  }
  .sidebar.toggled {
    overflow: visible;
    width: 90px !important;
  }
  .sidebar.toggled .nav-item:last-child {
    margin-bottom: 1rem;
  }
  .sidebar.toggled .nav-item .nav-link {
    text-align: center;
    padding: 0.75rem 1rem;
    width: 90px;
  }
  .sidebar.toggled .nav-item .nav-link span {
    font-size: 0.65rem;
    display: block;
  }
  .sidebar.toggled .nav-item .dropdown-menu {
    position: absolute !important;
    -webkit-transform: none !important;
    transform: none !important;
    left: calc(90px + 0.5rem) !important;
    margin: 0;
  }
  .sidebar.toggled .nav-item .dropdown-menu.dropup {
    bottom: 0;
    top: auto !important;
  }
  .sidebar.toggled .nav-item.dropdown .dropdown-toggle::after {
    display: none;
  }
}

.sidebar.fixed-top {
  top: 56px;
  height: calc(100vh - 56px);
  overflow-y: auto;
}